---
description: Reveal a card's full number and CVV (Claude Code only)
---

Reveal the full PAN and CVV for one of my avici cards using the avici connector.
Ask me which card first. Note: this requires a local terminal (Node) to decrypt,
so it only works in Claude Code — not the web app.
